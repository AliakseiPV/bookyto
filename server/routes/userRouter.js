const Router = require('express')
const router = new Router()

router.post('/registration', )
router.post('/login',)
router.get('/auth',)
router.post('/profile',)
router.get('/profile')

module.exports = router;

